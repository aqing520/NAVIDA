# VLN 历史图像选择实验记录

## 实验设置

- 模型：`models/qwen3vl4b_daggerv4_ckpt11000`
- 数据集：R2R `val_unseen`
- 评测方式：vLLM serving，同卡 12 worker
- 记录时间：2026-05-28

## Baseline 历史帧策略

基线使用 `uniform` 历史帧选择策略，也就是在历史观察序列中均匀抽取最多 8 张图像。

具体保留方式：

- 每一步将当前 RGB 帧加入 `rgb_list`
- 构造输入时，当前帧单独作为 `current observation`
- 历史帧从 `rgb_list[:-1]` 中选取最多 8 张
- 当历史帧数量不超过 8 时，保留全部历史帧
- 当历史帧数量超过 8 时，在完整历史窗口中均匀采样 8 张
- 采样包含历史窗口的起点和终点，因此能覆盖早期、中段和当前位置附近的观察
- trace 中保留 `history_selection` 和 `selected_history_indices`，用于检查每一步实际选中的历史帧索引

设计动机：

- 保留长程路线进展信息，帮助模型判断已经经过哪些区域和当前任务阶段
- 避免只关注最近局部帧导致历史上下文退化为短时间连续观察
- 作为当前历史图像选择实验的默认对照方案

## Baseline 评测结果

该实验为 baseline 历史帧策略的完整评测，使用旧版评测流程（无 `--history-selection` 参数，默认 uniform 行为）。

- 结果目录：`eval_log/qwen3_v2_r2r_rxr/qwen3vl4b_daggerv4_ckpt11000_r2r_valunseen_vllm_gpu1w12`
- 完成数量：1839 / 1839
- SR：56.50
- OSR：63.62
- SPL：51.39
- DTG：4.776
- Path Length：10.367
- 平均步数：79.79
- 中位步数：62
- P90 步数：126
- 最大步数：555
- 平均模型调用次数：30.20
- 中位模型调用次数：23

## Uniform 完整评测结果

该实验是对 baseline 历史帧策略的复跑，用于确认当前评测流程和历史帧记录逻辑不改变原始策略行为。

- 结果目录：`eval_log/qwen3_v2_r2r_rxr/qwen3vl4b_daggerv4_ckpt11000_r2r_valunseen_uniform_vllm_gpu0w12`
- 完成数量：1839 / 1839
- SR：57.59
- OSR：64.65
- SPL：52.42
- DTG：4.875
- Path Length：10.259
- 平均步数：79.28
- 中位步数：62
- P90 步数：126
- 最大步数：402
- 平均模型调用次数：30.09
- 中位模型调用次数：23

说明：

- `uniform` 与 baseline 的历史图像保留策略一致，都是保留历史窗口首尾并在中间均匀采样。
- 该结果相比同 ckpt 的旧 baseline 补齐结果略高，主要可视为 vLLM 随机采样带来的复跑波动，而不是新策略收益。

## Recency 历史帧策略

设计思路：只保留最近的历史视觉上下文，用最近连续帧替代全局均匀采样，希望模型更关注当前位置附近的局部状态。

具体保留方式：

- 每一步将当前 RGB 帧加入 `rgb_list`
- 构造输入时，当前帧单独作为 `current observation`
- 历史帧从 `rgb_list[:-1]` 中选取最多 8 张
- 当历史帧数量超过 8 时，选择最近 8 张历史帧
- trace 中保留 `history_selection` 和 `selected_history_indices`，用于检查每一步实际选中的历史帧索引

与 uniform 策略的区别：

- `uniform`：在完整历史窗口中均匀选 8 张，覆盖早期、中段和当前位置附近
- `recency`：只选最近 8 张，强调局部连续观察，但会丢失更长程的路线进展信息

## Recency 临时评测结果

该实验已提前停止。停止原因是临时结果明显较差，并且大量 episode 走到 early stop 上限，继续完整评测价值不高。

- 结果目录：`eval_log/qwen3_v2_r2r_rxr/qwen3vl4b_daggerv4_ckpt11000_r2r_valunseen_recency_vllm_gpu1w12`
- 完成数量：94 / 1839
- SR：23.40
- OSR：80.85
- SPL：7.58
- DTG：9.29
- Path Length：46.09
- 平均步数：340.88
- 中位步数：402
- P90 步数：402
- 最大步数：402
- 平均模型调用次数：122.59
- 中位模型调用次数：139.5

初步结论：

- `recency` 的单次输入图片数量与 `uniform` 基本一致，因此速度变慢主要不是推理开销增加。
- 慢的直接原因是轨迹明显变长：大量 episode 不主动 stop，最终被 400 步 early stop 截断。
- OSR 高但 SR/SPL 低，说明模型经常接近或路过目标附近，但缺少稳定停止和全局进度判断。
- 该结果支持”历史帧选择会造成分布差距”的判断：只看最近局部帧会丢失长程路径记忆，可能破坏阶段判断和 stop 决策。

## Anchor Recent 历史帧策略

设计思路：保留首帧（全局锚点）、最近若干帧（局部上下文），再补中间均匀帧，兼顾长程起点信息和近期细节。

具体保留方式：

- 首帧（index 0）和尾帧（最后一个历史帧）作为锚点始终保留
- 最近 4 帧历史帧保留
- 剩余名额从中间均匀采样
- 当历史帧不足 n 张时全部保留

与 uniform 策略的区别：

- `uniform`：在完整历史窗口中均匀选 8 张，覆盖均匀
- `anchor_recent`：优先保留首尾和最近帧，中间帧稀疏采样，可能丢失关键中间路径信息

## Anchor Recent 临时评测结果

该实验已提前停止。停止原因是临时结果极差，SR 为 0%，继续评测价值不高。

- 结果目录：`eval_log/qwen3_v2_r2r_rxr/qwen3vl4b_daggerv4_ckpt11000_r2r_valunseen_anchor_recent_vllm_gpu0w12`
- 完成数量：135 / 1839
- SR：0.00
- OSR：81.48
- DTG：9.12
- 平均步数：326.64
- 中位步数：402
- P90 步数：402
- 最大步数：402

初步结论：

- 几乎所有 episode 都走到 402 步 early stop 上限，模型完全无法做出正确决策。
- OSR 高达 81.48% 但 SR 为 0%，说明模型经常路过目标附近但无法正确停止。
- 保留首帧作为锚点的思路在理论上合理，但实际效果极差，可能是因为首帧与当前位置差异太大，反而干扰了模型判断。
- 该策略与 recency 类似，都因丢失中间路径信息导致失败。

## Pixel Novelty 历史帧策略

设计思路：按视觉变化量选帧，保留场景变化最大的历史帧，希望模型关注关键转折点。

具体保留方式：

- 首帧和尾帧始终保留
- 将历史帧缩放到 64x64，计算相邻帧的像素绝对差均值
- 选择变化最大的若干帧（最多 n-2 张）
- 不足时回退到均匀采样

与 uniform 策略的区别：

- `uniform`：均匀采样，不考虑内容变化
- `pixel_novelty`：优先选视觉变化大的帧，可能遗漏变化小但信息量大的帧

## Pixel Novelty 临时评测结果

该实验已提前停止。停止原因是临时结果极差，SR 为 0%，继续评测价值不高。

- 结果目录：`eval_log/qwen3_v2_r2r_rxr/qwen3vl4b_daggerv4_ckpt11000_r2r_valunseen_pixel_novelty_vllm_gpu1w12`
- 完成数量：304 / 1839
- SR：0.00
- OSR：70.39
- DTG：7.63
- 平均步数：171.34
- 中位步数：122
- P90 步数：402
- 最大步数：402

初步结论：

- 比 anchor_recent 稍好（DTG 更低，步数更少），但 SR 仍然是 0%。
- OSR 70.39% 低于 anchor_recent 的 81.48%，说明像素差分选帧可能遗漏了更多关键路径信息。
- 像素差分选帧的假设（变化大的帧更重要）不成立，可能是因为视觉变化大的帧不一定包含导航关键信息。
- 该策略同样丢失了均匀采样的路径覆盖优势。

## 帧数消融实验：16 帧（不重新训练）

基于原论文（NaVIDA）的消融实验，他们测试了 8 帧 vs 16 帧，结论是"8 帧够用，16 帧没提升"。但他们的模型 SR 只有 40.5%，我们的模型更强（SR 57.6%），所以直接推理时测试 16 帧是否有效。

实验设置：

- 策略：uniform（均匀采样）
- 历史帧数：16（从默认 8 改为 16）
- 其他设置与 baseline 一致
- **未重新训练**，直接在推理时增加帧数

## 16 帧临时评测结果

该实验已提前停止。停止原因是 SR 为 0%，性能严重退化。

- 结果目录：`eval_log/qwen3_v2_r2r_rxr/qwen3vl4b_daggerv4_ckpt11000_r2r_valunseen_uniform_16frames_vllm_gpu1w12`
- 完成数量：1207 / 1839
- SR：0.00
- OSR：64.13
- DTG：6.73
- 平均步数：131.33
- 中位步数：102
- P90 步数：243
- 最大步数：402

初步结论：

- SR 直接崩到 0%，但 OSR（64.13%）与 8 帧 baseline（64.65%）几乎一样。
- 这说明 16 帧模型"找得到目标，但不会停"——导航能力还在，但 stop 决策被破坏。
- OSR-SR 差距从 8 帧的 7% 扩大到 64%，说明所有路过目标的 episode 都无法正确停止。
- 验证了原作者的结论：推理时直接加帧数（不重新训练）没有帮助，反而有害。
- 模型训练时学的是"8 帧历史模式下的 stop 决策"，给 16 帧后这个模式被打破。
- **要真正利用更多帧，需要在训练时就用对应帧数**。

## 实验总结

| 策略 | SR | OSR | SPL | DTG | 平均步数 | 状态 |
|------|-----|-----|-----|-----|---------|------|
| baseline (uniform, 8帧) | 56.50 | 63.62 | 51.39 | 4.78 | 80 | 完成 |
| uniform (复跑, 8帧) | 57.59 | 64.65 | 52.42 | 4.88 | 79 | 完成 |
| uniform (16帧, 不训练) | 0.00 | 64.13 | - | 6.73 | 131 | 提前停止 |
| recency | 23.40 | 80.85 | 7.58 | 9.29 | 341 | 提前停止 |
| anchor_recent | 0.00 | 81.48 | - | 9.12 | 327 | 提前停止 |
| pixel_novelty | 0.00 | 70.39 | - | 7.63 | 171 | 提前停止 |

关键发现：

1. **uniform 采样是最优的简单策略**：均匀覆盖历史窗口，保证模型看到路径的各个阶段。
2. **偏向特定帧的策略都失败了**：recency（只看最近）、anchor_recent（保留首尾+最近）、pixel_novelty（选变化大的帧）都导致 SR 大幅下降或归零。
3. **根本原因**：模型依赖历史帧来判断”我已经走过哪些地方”和”当前处于什么阶段”，偏向特定帧会破坏这种全局路径记忆。
4. **推理时加帧数无效**：16 帧（不重新训练）直接崩到 SR 0%，验证了原作者结论。模型训练时学的是 8 帧分布，推理时给 16 帧会破坏 stop 决策。
5. **下一步方向**：要利用更多帧，必须在训练时就用对应帧数。或者尝试其他方向（对话式历史、文本记忆等）。
